spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banking_product_systems_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true


kind: Application


